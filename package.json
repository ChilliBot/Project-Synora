{
  "name": "project-synora",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "corepack pnpm -r build",
    "test": "corepack pnpm -r test",
    "typecheck": "corepack pnpm -r typecheck",
    "dev": "corepack pnpm --parallel --filter @synora/web --filter @synora/api --filter @synora/representation-worker dev"
  }
}
